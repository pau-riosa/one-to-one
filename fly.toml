# fly.toml file generated for quiet-lake-732 on 2022-08-10T22:54:51+08:00

app = "quiet-lake-732"
kill_signal = "SIGTERM"
kill_timeout = 5
processes = []

[deploy]
  release_command = "/app/bin/migrate"

[env]
  PHX_HOST = "quiet-lake-732.fly.dev"
  PORT = "8080"
  VIRTUAL_HOST = "quiet-lake-732.fly.dev"
  USE_TLS = "true"
  USE_INTEGRATED_TURN = "false"
  KEY_FILE_PATH=""
  CERT_FILE_PATH=""

[experimental]
  allowed_public_ports = []
  auto_rollback = true

[[services]]
  http_checks = []
  internal_port = 8080
  processes = ["app"]
  protocol = "tcp"
  script_checks = []
  [services.concurrency]
    hard_limit = 25
    soft_limit = 20
    type = "connections"

  [[services.ports]]
    force_https = true
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

  [[services.tcp_checks]]
    grace_period = "1s"
    interval = "15s"
    restart_limit = 0
    timeout = "2s"
